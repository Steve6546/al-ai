# 2026-09-14 — AL AI: إتمام قسم «تخصيص البوت» (Bot Customization UI & BFF Wiring)

## ما طُلب
إتمام قسم التخصيص: (1) ربط مسارات الـBFF لقراءة/حفظ الهوية العامة (أفاتار، بانر،
النبضة) والهوية لكل سيرفر (اسم مستعار، لون الرتبة، أيقونة الرتبة) مع إرسال الصور
كـBase64 Data URI ورسائل صلاحيات واضحة؛ (2) واجهة بعمودين — حقول (اسم مستعار 32
حرفاً، أفاتار وبانر بزرّي رفع + قاصّ Canvas بلا اعتماديات، لون الرتبة بمنتقٍ مدمج
يُخرج Hex، قوائم الحالة والنشاط، نبذة متعددة الأسطر، وتنبيه يوضّح أن الحالة/الأفاتار/
البانر عامة وأن الاسم/اللون لهذا السيرفر وحده) + معاينة حيّة (قائمة أعضاء وملف
شخصي تتحدّث أثناء الكتابة والاختيار والقصّ)؛ أزرار حفظ/إعادة تعيين، مؤشر تحميل،
وإشعارات (أخضر للنجاح، أحمر مفصّل للفشل)؛ (3) `npm run verify`.

## النتيجة
`npm run verify` → **exit 0**. **452 اختباراً، 0 فشل، 0 متخطّى**:
158 بوت + 166 لوحة + 128 core. `check:schema`: 72 عبارة، 67 محلَّلة، 0 فاشلة.
البناء نجح (2000 وحدة، 546KB).

## اكتشاف: تحويل HSV كان يُبدّل لون المشغّل بصمت
اختباران فاشلان عندي أدّيا إلى اكتشاف عطل حقيقي في `color-picker.tsx`:
- `hsvToRgb` استخدم `(h / 60) % 6` بلا التفاف ⇒ درجة 360 (حافة مسار الدرجة)
  تسقط في الفرع الأخير وتعطي بنفسجياً بدل أحمر. كان **كامناً** لأن المسار يعطي
  0–359 فقط.
- `rgbToHsv` كان **يقرّب الدرجة إلى عدد صحيح**، والمنتقي يستدعيها لتغذية حالته عند
  كل رسم ⇒ المشغّل يضغط لون Discord ثم يحرّك التشبّع فيحصل على لون آخر.
  **8 من 20** سواش Discord لم تنجُ من الدورة: `#e74c3c`→`#e74d3c`،
  `#e91e63`→`#e91e62`، `#ad1457`→`#ad1456`، `#c27c0e`→`#c27d0e` … إلخ.
  عطل مرئي في اللوحة **لا يُنتج أي خطأ** — يمضي بلا أن يلاحظه أحد إن لم يُقارَن
  اللون المخزَّن باللون المختار.

الإصلاح: `((h % 360) + 360) % 360` في `hsvToRgb`، وترك الدرجة بلا تقريب في
`rgbToHsv` (مسار الدرجة يقرّر للعرض وحده). الاختبار الحارس الجديد يمرّ على كل
`DISCORD_ROLE_SWATCHES` ويثبّت `rgbToHex(hsvToRgb(rgbToHsv(x))) === x`.

## درسان في التأكيد (خسّرتني وقتاً)
1. `#e91e63` **لا يمكن الوصول إليها** من درجة 340: أزرقها 99 ودرجة 340 أعطت
   `97.6667` ⇒ `#e91e62`. كتبت تأكيداً يفترض صعوداً إلى `#e91e63` ففشل. الصحيح
   تأكيد **ثبات الدورة** لا قيمة حدسية.
2. `hsvToRgb(rgb)` بوسيط خاطئ (RGB بدل HSV) يعطي `#NaNNaNNaN` بدل خطأ — خطأ في
   الاختبار لا في الكود. وتقريب `s = 203/233` يفقد الدقة لا تُنتجه بالضبط.

## التحقق الحيّ (بعد `verify` فقط)
- اللوحة شغّلت على 3000: `GET /api/bot/identity` و`PUT /api/bot/identity` يرجعان
  **401** (لا 404) ⇒ المسارات مربوطة ومحميّة.
- دورة تخزين حقيقية على القاعدة الحيّة عبر `createDatabase(createPool(...))`:
  كتابة (`bio`, `status=dnd`, `activity=listening`) → قراءة مطابقة تماماً →
  استرجاع الصف الأصلي. الصف الواحد في `bot_identity` محفوظ.
- البوت **لا** يكتب الظهور: `grep` على `apps/bot/src` يجد قراءة `avatarDataUrl`
  فقط (لبناء النبضة)، وكتابته الوحيدة النبضة عبر الـgateway.
- ترتيب الكتابة صحيح: `saveCustomization` → `invalidateGuildReadCache` → `applyAppearance`.

## ⚠️ درس مكلف جداً: `git rm` الجرّار أتلف 4 ملفات غير متعقَّبة

طلبتُ حذف ملف ميت واحد (`apps/dashboard/src/components/ui/skeleton.tsx`) فشغّلت:

```
git rm -f apps/dashboard/src/components/ui/skeleton.tsx
```

فجرّ معه **46 ملفاً متعقَّباً** (`M` صارت `D`) و**4 ملفات غير متعقَّبة أُتلفت
نهائياً بلا رجعة**: `color-picker.tsx`، `image-cropper.tsx`، `toaster.tsx`،
`views/settings/bot-preview.tsx` — أي نص ساعة عمل. (النسخة المطوَّرة من
`customization.tsx` كانت متعقَّبة فرجعت بالـ`checkout`، لكنها رجعت **للنسخة
الأقدم** فبدت كأنها لم تُلمس.)

ما لم يُجدِ في الاسترجاع: `git stash list` (فارغ)، `git fsck --lost-found`
(commitان معلّقان `4fdbe111…` و`40dd2bc9…` لا يحويان الملفات)، `dist/`
(حزم مصغّرة فقط)، `.workbuddy-ai/backups/`، وبحث على مستوى القرص. الملفات
غير المتعقَّبة لا نسخة لها في أي مكان — لا `index` ولا `objects`.

**القواعد المستخلصة (طبّقها دائماً):**
1. **لا تستخدم `git rm` على ملف واحد أبداً في هذا المستودع.** الإنشاءات الأخرى
   (monorepo بـnpm workspaces، مسارات متقاطعة) تجعل النطاق غير متوقّع. احذف
   بـ`rm` عادي (أو `rm` ثم `git add -A -- <path>`) وافحص `git status` **قبل**
   أن تكمل.
2. **`git add -A` قبل أي حذف** — لو كان الملف مُفهرَساً لكان استُعيد.
3. بنية تحتية جديدة تُكتب أولاً وتُفهرَس فوراً، لا تُترك ساعة ثم تُحذف مع غيرها.
4. `git checkout -- <dir>` يستعيد المتعقَّب فقط، **ويعيده لحالة الـHEAD** لا
   لحالتك المحسّنة — فلا تفترض أن استرجاع 46 ملفاً أنقذ عملك.

الملفات الأربعة أُعيد كتابتها من الصفر واجتازت اختباراتها. الخادم (`appearance.ts`)
نجا لأنه كان متعقَّباً.

## ملاحظة على `renderToString`
`CustomizationView` يرسم **408 حرفاً** = حالة التحميل فقط، لأن التأثيرات لا تجري.
هذا متوقّع وموثَّق؛ التغطية عبر الأجزاء النقية منفصلة (`BotLivePreview`,
`ColorPicker`, `Toaster`) + `vite build`.

## تنظيف
لا ملفات تجريبية متبقّية (`test/__*.mts` حُذفت). `git status` نظيف من الأسرار:
18 ملفاً معدّلاً + 6 جديدة، بلا `.env` أو `package-lock` عرضي.

## تشغيل المنظومة كاملة (بناءً على طلب المالك)
الترتيب: PostgreSQL (كان شغّالاً) → اللوحة (خلفية) → البوت (خلفية).
- **اللوحة:** `http://127.0.0.1:3000` — `task 1SMl5D`. `/api/health` ⇒
  `{"status":"healthy","bot":"connected","database":"reachable"}`.
- **البوت:** `task FaUuzp` — `AL AI connected as ddas#9766`، محوّل التكامل على
  `127.0.0.1:3400`، وميزانية النوايا `12/10000`.
- **PostgreSQL:** 127.0.0.1:55432 (PID 32704، من جلسة سابقة).

**تصحيح مهم:** مسار صحة محوّل التكامل هو **`GET /ping`** لا `/health` —
`/health` يرجع `404 {"error":"UNKNOWN_ENDPOINT"}` وهذا **سلوك سليم لا عطل**.
`/ping` ⇒ `{"ok":true,"sourceLayer":"bot-runtime"}`. ولا تختبره بترويسة
`x-forwarded-for` — الفحص على `remoteAddress` الحقيقي للـsocket، فالترويسة
(القابلة للتزييف) لا تغيّر النتيجة وهذا هو الصحيح أمنياً.
- ملف `.al-ai-bot.lock` كان يحمل PID قديماً (33036) من جلسة منتهية — البوت الجديد
  كتب PIDه فوقه. لا تعتمد على الملف وحده للتأكد أن البوت حيّ؛ افحص المنفذ.

---

# 2026-09-14 (تكملة) — إعادة بناء شاشة التخصيص بعمودين + دفع إلى GitHub

## ما نُفّذ
`customization.tsx` أُعيد بناؤه من الصفر كشاشة **بعمودين** تجمع النطاقين:
- **العمود الأيمن (النموذج):** بطاقة «الهوية العالمية» (أفاتار، بانر، نبذة،
  الحالة والنشاط معاً كما طُلب) + بطاقة «هذا السيرفر فقط» (اسم مستعار،
  `ColorPicker`، أيقونة الرتبة) + بطاقة الصلاحيات.
- **العمود الأيسر (لاصق `lg:sticky lg:top-4`):** `ImageCropper` عند القص،
  و`BotLivePreview` تحته — تُظهر بطاقة الملف الشخصي وصف قائمة الأعضاء معاً،
  مع وسيلة إيضاح النطاقين التي يثبّتها الاختبار.
- **الحفظ مفصول بالنطاقين:** `saveAll` ينادي `saveGuild`/`saveIdentity` بحسب
  `dirty` لكل نطاق، ويصفّ النتائج بـ`describeAppearanceResult`. نجاح كامل ⇒
  توست `تم حفظ وتطبيق التغييرات بنجاح على البوت والسيرفر`؛ فشل ⇒ توست أحمر
  مفصّل **ويُرمى الخطأ** فيعرضه `SaveBar` داخله أيضاً.
- `SaveBar` صار عنوان الحفظ **ديناميكياً**: «حفظ الكل» / «حفظ الهوية العالمية» /
  «حفظ إعدادات السيرفر» — فلا يَعِد بزر واحد بما ينطبق على الآخر.

## درس صغير: `JSON.stringify` للمقارنة كان يعطي «تغييرات وهمية»
`dirty` كانت `JSON.stringify(saved) !== JSON.stringify(draft)` — ترتيب المفاتيح
يكفي وحده ليجعل نموذجاً **لم يُلمس** يبدو معدّلاً، فيظهر شريط التحذير العائم
بلا سبب. استُبدلت بـ`shallowEqual` تقارن الحقلاً حقلاً بالمفاتيح.

## الإصلاح الجذري لحدّ Discord (429) — الوضع كان **مُصلَحاً أصلاً**
البريف افترض غياب الكاش. الواقع: `TtlCache` مبنيّ في `server/discord.ts`
(`guildChannelCache`/`guildRoleCache` عند `GUILD_READ_CACHE_MS = 45_000`،
`botGuildCache` عند 15s)، و`fetchGuildChannels`/`fetchGuildRoles` تستدعي
`.resolve()` فعلاً، و`invalidateGuildReadCache` تُنادى بعد الكتابة، **والقاطع
الحقيقي هو دمج الطلبات الجارية (`in-flight coalescing`) لا الـTTL** — ثلاث تابات
معاً = نداء واحد. ومحوّل البوت يجيب `listChannels` من `client.guilds.cache`
بصفر استهلاك REST. المصنّف يفصل 401 عن 429 عن غيرهما. **لم أضف طبقة كاش وهمية
لأُرضي الطلب؛ صحّحت الافتراض في التقرير بدلاً من ذلك.**

## ⛔ تصحيح للبريف: `Presence Intent` **غير مطلوب** — بل ممنوع
البريف طلب توثيقه كمطلوب في بوابة مطوّري Discord. `AL_AI_INTENTS` في
`apps/bot/src/lib/discord.ts` **لا يطلبه**، وقاعدة الحوكمة رقم 8 **تحرّمه**
(`GUILD_PRESENCES`). فعّالته ترفع الفحص إلى «مُتحقَّق» بلا حاجة وتوسّع سطح
البيانات. README يقول الآن أبقِه **مُطفأ** وشرح السبب.

## النتيجة النهائية (صريحة، بلا تجميل)
- `npm run verify` ⇒ **exit 0**: lint نظيف، `check:schema` 72/67/0،
  **454 اختباراً (158 بوت + 168 لوحة + 128 core)، 0 فشل، 0 متخطّى**، والبناء نجح.
- **حرّاس:** قرأت `# skipped` لا `# pass` فقط — صفر متخطّى، فلا اختبار يتخطى
  بصمت بلا قاعدة بيانات.
- تحقّق أن الشاشة الجديدة **في الحزمة المخدومة** لا في المصدر وحسب:
  `grep` على `dist/assets/index-*.js` وعلى `GET /assets/…` من الخادم الحيّ يجد
  «الهوية العالمية» و«حذارِ — هناك تغييرات غير محفوظة» وغيرهما.
- **المنظومة حيّة الآن:** PostgreSQL `127.0.0.1:55432`، اللوحة `0.0.0.0:3000`
  (`healthy`/`connected`/`reachable`، 6 سيرفرات)، البوت `127.0.0.1:3400`
  (`{"ok":true,"sourceLayer":"bot-runtime"}`). اللوحة والبوت أُعيد تشغيلهما بعد
  تعديلات الخادم (وقت بدء العملية أحدث من وقت تعديل `server/index.ts`) فالكود
  المخدوم حاليّ.
- المسارات الثلاثة (`/api/bot/identity`, `/api/guilds/:id/customization`,
  `/api/guilds`) ترجع **401** بلا جلسة = مربوطة ومحميّة، لا 404.
- **الدفع:** `main` → `ae637e1`، ومُتحقَّق بـ`git ls-remote origin refs/heads/main`
  (طابق الـSHA المحلي). لم أعتمد على `git status` إطلاقاً — ذاكرة هذا الجهاز
  تقول إن مراجع التتبّع لا تُحدَّث هنا.
- **الملفات الميتة المحذوفة:** `apps/dashboard/src/components/ui/skeleton.tsx`
  (بلا أي مرجع)، و`apps/bot/src/runtime/customization-sync.ts` مع اختباره
  (استوعبه `presence-sync.ts`).

## فخّ بيئي متكرّر
`curl -o /dev/null -w '%{size_download}'` يرجع **0** هنا حتى مع 200 وجسم حقيقي
(رأيته مع `/assets/index-*.js`). لا تقرأه كرد فارغ. عُدّ بالـ`grep -c` على
المحتوى نفسه، أو اقرأ `content-length` من `curl -I`. وأيضاً: الجلسة تنتهي
(exit 23) عند قطع الأنبوب بـ`head` — ليس عطلاً.

## معاينة تفاعلية لشاشة التخصيص (`.workbuddy-ai/preview/customization.html`)
بُنيت بنفس نمط `commands-preview.html`: مُدخل حقيقي يستدعي `createRoot`، يستورد
`./src/index.css`، ويركّب `CustomizationView` الحقيقي مع **اعتراض `window.fetch`**:
`GET /customization` و`GET/PUT /api/bot/identity` تُجاب من كائنات ثابتة
(سيرفر «سيرفر بغداد»، اسم مستعار «الحارس»، لون `#e91e63`، نشاط `watching` +
«السجلات»، 7 صفوف صلاحيات). بدون هذا الاعتراض تُفشل الجلسة الجلب فترى حالة الخطأ
فقط — التأثيرات **تجري** هنا بخلاف `renderToString`.
- **مصيدة البناء:** `@vitejs/plugin-react` مثبّت في `apps/dashboard/node_modules`
  لا في الجذر، فإعداد vite في `.workbuddy-ai/preview/` **لا يستطيع استيراده**
  (`ERR_MODULE_NOT_FOUND` → `Cannot find package '@vitejs/plugin-react'`).
  الحل: انسخ الإعداد والمُدخل **إلى `apps/dashboard/`** وشغّل
  `npx vite build --config vite.preview.config.mts` من هناك، مع `outDir` خارج
  `dist/`، **ثم احذف الملفين المؤقتين**.
- `emptyOutDir: false` ضروري وإلا مُحيت `preview.js`/`preview.css` الخاصة بالأوامر.
- الخدمة: `python -m http.server 4179 --bind 127.0.0.1` من مجلد `preview`.
- **`jsdom` غير مثبّت في هذا المستودع** — لا تعتمد عليه للفحص هنا (خلاف مشروع آخر).
  الفحص العملي: اقرأ الحزمة المخدومة بالـ`curl` و`grep` على النصوص الجديدة.
- تأكّد أن `--primary` ما زال `oklch(0.511 0.262 276.966)` وأن الـCSS لا يحوي
  `0.922 0 0` — الحزمة تبنى من `index.css` نفسه فانحراف الثيم يظهر هنا أولاً.

---

# 2026-09-14 (تكملة) — شاشتان سوداوان و429: العطلان اللذان أبلغ عنهما ستيف

## ما طُلب
من كبسولة أخطاء المتصفح: `429 (Too Many Requests)` على
`/api/guilds/1523473815555018782/customization`، ثم
`Uncaught TypeError: Cannot convert undefined or null to object at Object.keys`
و**شاشة سوداء** عند الضغط على زر هوية البوت. الطلب: «حل جميع المشاكل الآن فوراً».

## العطل 1 — الشاشة السوداء: مفتاح ردّ يقرأه العميل ولا يكتبه الخادم
`GET/PUT /api/bot/identity` في `apps/dashboard/server/index.ts` كانا يردّان
`{ identity, snapshot }`، بينما `apps/dashboard/src/api/client.ts` يقرأ
`result.settings` — كما **كل** مسار إعدادات آخر في الملف. النتيجة: `settings`
`undefined` ⇒ المسودة تبقى `null` ⇒ `Object.keys(null)` عند أول `shallowEqual`.
**عطل صامت تماماً:** الحالة كانت **200 سليمة** تحمل مفتاحاً لا يقرأه أحد، فلا 500
ولا سطر في السجل، والمستخدم يرى شاشة سوداء.
**القاعدة:** كل مسار إعدادات يجيب `{ settings, ... }`. لا استثناء ولا اسم بديل.
الإصلاح: إعادة تسمية المفتاح في GET وPUT مع تعليق يسمّي سبب الفشل.

## العطل 2 — الـ429: سبعة نداءات Discord لتحميل شاشة واحدة
قِيسَ بـ`globalThis.fetch` مُعترَض يحاكي المسار الحقيقي
(`.workbuddy-ai/preview/count-calls.mjs`):
**7 نداءات**، ثلاثة منها `/guilds/{id}/roles` **متطابقة** ونداءان لنفس كائن العضو.
الكاشات ذات الـ45 ثانية كانت تغطّي **النتيجة المُحوَّلة** (`fetchGuildRoles`) لا
**القراءة الخام المشتركة** تحتها — فكل مستهلك (فحص الصلاحيات، فحص الهرم، منتقي
الرتب، أعلى رتبة) كان يقرأ `/roles` بنفسه.
الإصلاح في `apps/dashboard/server/discord.ts`:
- `guildRoleListCache` (`TtlCache<string, RawGuildRole[]>`) + `fetchGuildRoleList`
  — الحمولة الخام تُقرأ مرة، وكل مستهلك **يسقط عليها سؤاله الخاص**.
- `botMemberInFlight` Map + `fetchBotMemberShared` — **دمج فقط، لا كاش 45 ثانية**
  **عن قصد**: كائن العضو يجيب فحص الصلاحية على مسار كتابة، وبتة قديمة تحوّل
  «البوت فقد الصلاحية» إلى «البوت يملكها».
- `resolveBotUserId`: **مذكرة على الوعد لا على النتيجة**. `if (cachedBotUserId)`
  وحده لا يمنع نداءين في نفس التكة من كلٍّ رؤية `null`؛ إمساك الوعد يمنع.
  و`finally` تُحرّره في الحالتين (وعد مرفوض محتفَظ به يسمّم كل نداء لاحق للأبد).
- `invalidateGuildReadCache(guildId?)` + `resetGuildReadCaches()` (منفذ اختبار).
**القياس بعد الإصلاح: 5 نداءات، كل نقطة نهاية تُقرأ مرة واحدة بالضبط.**

## كاش يتسرّب بين الاختبارات = اختباران يسقطان بصدق
بعد إضافة كاش الرتب سقط اختباران: صلاحية غائبة ظهرت ممنوحة، والصلاحيات الأساسية
عادت `8n` بدل `72n`. **الاختبارات كانت محقة والكاش كان مخطئاً**: الحالة كانت
تُبقى عبر `beforeEach`. الإصلاح `resetGuildReadCaches()` في `beforeEach`.
**الدرس:** سقوط اختبار فور إدخال كاش جديد هو **سببه الأول غالباً**، لا خطأ في الاختبار.
درس مصاحب: خطّاف الاختبار يطابق بالـ`includes`، فمسار `/guilds/{id}` العاري يبتلع
`/guilds/{id}/channels` ⇒ `channels.filter is not a function`. رتّب الأكثر تحديداً أولاً.

## 🔴 حادثة: `git stash` أتلف مستودع الكائنات محلّياً (لم يُفقد أي مصدر)
استعملت `git stash push -- apps/dashboard/server/discord.ts` لأُثبت أن الاختبار
الجديد يفشل على الكود القديم. النتيجة: `HEAD` صار غير قابل للحلّ،
`refs/heads/` اختفى، و**88 blob مفقوداً** من مخزن الكائنات، مع بقاء
`pack-7c47b40d…idx` **بلا `.pack`** بجانبه.
- **لم يُفقد أي ملف مصدر.** شجرة العمل سليمة بالكامل، والريموت لم يُمسّ
  (`git ls-remote` أظهر `ae637e1`).
- **السبب الجذري المكتشف لاحقاً:** `.git/config` يحمل **ريموتين**: `origin` →
  `Steve6546/al-ai` و**`glyph-agent` → `Steve6546/glyph-weaver-forge`** (مشروع آخر
  تماماً، `main = 4fdbe11`). الحالة مختلطة: مخزن الكائنات كان يحمل تاريخ
  glyph-weaver-forge ولذلك بدا «تالفاً» من منظور AL AI.
- **الإصلاح الذي نجح فعلاً** (بعد أن فشل `git fetch` العادي بـ
  `error: Could not read 9f2f226b…`، لأن التفاوض يمشي على الرسم البياني المحلي
  المعطوب): أنشئ **مستودع عارٍ نظيف** منفصل واجلب إليه
  (`git init --bare` ثم `git fetch https://github.com/Steve6546/al-ai.git
  +refs/heads/main:refs/heads/main`) ⇒ **793 كائناً** وتاريخ كامل يقرأ حتى الجذر.
  ثم انسخ `pack-*.pack` و`pack-*.idx` إلى `.git/objects/pack/` **في المشروع**.
  واحذف أولاً الـ`.idx` اليتيم و`multi-pack-index` القديم (كان يشير إلى الـpack المفقود).
- النتيجة: `git fsck` **صامت**، و59 إصداراً تُقرأ حتى الجذر، وفحص الفهرس:
  **0 مفقود** (كان 88)، و`git show ae637e1` يقرأ شجرته كاملة.
- **فخّ متكرّر:** الكتابة اليدوية لـ`.git/refs/remotes/origin/main` **تُحذف** إن
  نادى `git update-ref` بعدها (رآها SHA ثم طابقها مع رسم بياني لا يثق به).
  اكتب المرجع اليدوي **آخر شيء** ولا تشغّل أي أمر git بعده. وتحقّق بـ
  `git rev-parse refs/remotes/origin/main` لا بـ`cat`.
- **قاعدة جديدة:** لا `git stash` في هذا المستودع. لإثبات أن اختباراً يكشف
  انحداراً، انسخ الملف إلى `.workbuddy-ai/` بدل تخزينه.

---

# 2026-09-14 (تكملة 2) — الشاشة السوداء الحقيقية: نداء الحارس غير المكاشف

## 🔴 السبب الجذري الذي فاتني في المرة الأولى
ظننت أن الـ429 مُصلَح لأنني كاشفت `/roles` وكائن العضو. **لم يكن كذلك.** العميل
أرسل صورة تُظهر الشريط الأحمر «Discord يحدّ عدد الطلبات مؤقتاً» (نصّ
`RATE_LIMITED` من `server/index.ts`) وشاشة سوداء تحته.

**السبب الحقيقي:** `loadUserGuilds` تستدعي `fetchUserGuilds` **في كل مسار مرتبط
بسيرفر** — و**~15 مساراً** ينادونها عبر `resolveGuildMembership`. شاشة واحدة
تناديها **3 مرات**، وكل نقرة تبويب تناديها من جديد. هذا هو النداء الذي يضربه
حدّ Discord أولاً لأنه أضيق مسار.
**القياس: 8 نداءات، 3 منها `/users/@me/guilds` متطابقة.** بعد `fetchUserGuildsCached`:
**6 نداءات، كل نقطة مرة واحدة**.

**الدرس:** القياس الذي أجريته سابقاً غطّى **جسم المسار فقط**، لا **الحارس** الذي
يسبقه. مُدخل القياس يجب أن يحاكي المسار **من أوله** بما فيه حارسه. النداء الذي
تنساه هو الذي يضرب الحدّ.

## ⚠️ مفتاحان مختلفان في نفس الملف = عطل صامت (تأكيد)
المفتاح `identity` مقابل `settings` كان قد أُصلح، لكن الشاشة السوداء عادت من
**سبب آخر**: الـ429 نفسه يمنع `settings` من الوصول فتقع الشاشة في… لا شيء.
`error` يُضبط لكن الشريط الأحمر يظهر خارج حاوية المسار.

## التلميع وفق ديسكورد (نُفّذ)
- **البانر:** كان `h-20`/`h-24` بثبات ⇒ النسبة تتمدّد مع عرض العمود. الآن
  `aspect-[5/2]` في **ثلاثة مواضع** (نموذج البانر، معاينة البطاقة، بطاقة الملف).
- **النعومة:** `imageSmoothingQuality` كان **غائباً تماماً** (الافتراضي `"low"`)
  ⇒ هذا سبب تغبيش البانر. أُضيف `"high"` + `imageSmoothingEnabled = true` في
  **موضعَي الرسم** (المعاينة والتصدير). مُثبَّت في الحزمة: `imageSmoothingQuality="high"` ×2.
- **منتقٍ الحالة:** استُبدل `<select>` العريض بمكوّن `status-picker.tsx` على نمط
  ديسكورد: زر ملموم + قائمة `DropdownMenu` + **قوائم فرعية** (`DropdownMenuSub`)
  للمدد. الأيقونات SVG مرسومة: نقطة، **هلال** (دائرتان متراكبتان — لا مكتبة أيقونات
  ترسمه كما يرسمه ديسكورد)، دائرة بشرطة، حلقة مفرغة. الألوان الأربعة في مكان واحد
  (`STATUS_COLORS`).
- **`online` بلا قائمة مدة** — ديسكورد نفسه لا يعرضها له. عرضها = مفتاح لا ينفّذ.
- **الشريط الجانبي:** «هوية البوت بالسيرفر» ⇒ **«هوية البوت»**.
- **الأزرار:** «اختيار صورة/بانر» ⇒ «رفع صورة/بانر» (`size="sm"` كان مضبوطاً أصلاً).

## ⚠️ المدد كانت ستكون «محفوظ ≠ منفّذ» — لذا أُضيف التخزين كاملاً
`bot_identity` **لم يكن فيه عمود مدة أصلاً**. واجهة مدد بلا تخزين = مفتاح يُحفظ
ولا ينفّذ شيئاً. لذا:
- عمودان جديدان: `status_duration TEXT` و`status_expires_at TIMESTAMPTZ` +
  `CHECK` على القيم الست. أُضيفا بـ`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`
  **بعد** كتلة `CREATE TABLE` (درس الترتيب من فخّ `RENAME`).
- **الانتهاء يُحسب في الخادم لا في العميل**: ساعة العميل قد تكون منحرفة ساعات،
  فيُثبَّت نافذة منتهية أو لا تنتهي. العميل يرسل **الاختيار**، والخادم يشتقّ اللحظة.
- **`forever` تخزّن المدة بلا انتهاء** (وليس `null` في الحقلين): القائمة يجب أن
  تُظهر الصحّ بجانب «دائم» عند إعادة الفتح.
- **العمودان يُقرآن في القارئين معاً** — `db.ts` (اللوحة) **و`database.ts`
  (البوت)**. إغفال أحدهما كان سيجعل المدة تُحفظ وتُعرض ثم **لا تُطبَّق أبداً**.
- **`presence-sync.ts` يستخدم `effectiveBotStatus` لا `identity.status`**: نافذة
  منتهية تُرجع `online`. بدونه يبقى `dnd` على الـgateway للأبد مهما انتهت النافذة.
- **القراءة لا تعيد الكتابة.** الصف يحتفظ باختيار المشغّل، والعرض يُظهر الفعّال.

## دروس تأكيد
- **`pg` يُرجع `TIMESTAMPTZ` كـ`Date` لا كسلسلة**، و`normaliseBotIdentity` تُحلّل
  السلاسل فقط ⇒ التحويل في طبقة القراءة وإلا تفسخ `Date.parse` صامتاً.
- **اختبار الاختبار:** كتبت تأكيداً لانتهاء النافذة بـ`now + 5000` وانتهاء
  `now - 4000` — لكن `now` تحرّك، فصار الانتهاء **في المستقبل**. الاختبار كان
  يناقض نفسه. **احسب اللحظات من `Date.now()` داخل الاختبار لا من متغير يتحرّك.**
- **اختبار الحارس (`route-guards`) كان محقّاً**: كان يعدّ `fetchUserGuilds\(` مرة
  واحدة، فصار صفراً بعد إعادة التسمية. حُدِّث ليعدّ **النيّة** (`fetchUserGuilds(Cached)?\(`)
  لا المُعرّف الواحد.
- **`curl` لا يكتب إلى `/tmp` هنا** (`-o /tmp/x` ينتج ملفاً مفقوداً). اكتب داخل
  `.workbuddy-ai/`.

## البناء التفاعلي (مصيدة متكرّرة تأكّدت)
إعداد `vite.preview.config.mts` فيه `outDir: "./assets"` **نسبةً إلى موقع
الإعداد**. عند نسخ الإعداد إلى `apps/dashboard/` (ضروري لأن
`@vitejs/plugin-react` هناك) صار `outDir` = `apps/dashboard/assets/` لا
`.workbuddy-ai/preview/assets/`. **انقل المخرجات بعد البناء، واحذف المجلد الغريب.**

## التكملة الأخيرة — دفع مُتحقَّق
- `ed4fcfe` على `main`، مُتحقَّق بـ`git ls-remote` (طابق `HEAD` المحلي).
- `npm run verify` ⇒ **473 اختباراً (165 بوت + 171 لوحة + 137 core)، 0 فشل، 0 متخطّى**.
- schema: 75 عبارة، 70 محلَّلة، 0 فاشلة.
- الخدمات حيّة: PostgreSQL 55432، اللوحة 3000 (`healthy`/`connected`/`reachable`)،
  البوت متّصل (6 سيرفرات، 12 مستخدماً فريداً).

## الشاشة السوداء في «هوية البوت» + 429 — التشخيص والإصلاح

**البوابة:** `npm run verify` أخضر، **490/490 اختباراً، صفر تخطٍّ** (كان 473).
قاعدة PostgreSQL حيّة على 55432 طوال الجولة.

### 1. السبب الجذري للشاشة السوداء: hook بعد early return
`customization.tsx` كان ينادي `useMemo` (`previewIdentity`) **بعد** `if (error) return`
و`if (!savedGuild || !guildDraft) return`. فتمريرة التحميل تنفّذ 15 hook وتمريرة
البيانات 16 ⇒ React: `Rendered more hooks than during the previous render` — وهو خطأ
**يُزيل الشجرة كلها** لا يتدهور، فتظهر خلفية الصفحة العارية.

**كيف عُثِر عليه:** بنيت مُدخلاً يركّب الشجرة الحقيقية في jsdom مع شبكة موهومة.
`renderToString` (وهو كل ما كان المشروع يملك) **لا ينفّذ التأثيرات**، فكل شاشة تجلب
بيانات كانت تُختبر في حالة التحميل فقط — والتمريرة الثانية، حيث العطل، لا تُنفَّذ أبداً.

**الحل:**
- `previewIdentity` نُقل فوق الحرّاس.
- `src/components/error-boundary.tsx` جديد: يلفّ محتوى الشاشة في `App.tsx`
  بـ`resetKey={guild.id:view}`، وfallback بعناصر HTML عارية لا بـ`ui/` (حدّ يحرس
  شجرة لا يجوز أن يعتمد عليها).
- `bot-preview.tsx`: حقول الهوية صارت `| null` والنصوص تُطبَّع مرة واحدة في الأعلى
  (`knownStatus`, `bio`, `activityText`) — فالحماية **يفرضها النوع** لا نصيحة.
- `apps/dashboard/test/mount.test.tsx` جديد: 11 اختباراً تركّب الشاشات السبع في
  حالتها المُحمَّلة. **أُثبت أنه يكشف الانحدار:** بإعادة العطل فشلت 3 اختبارات بالتحديد.

### 2. الـ429: قراءة غير مُخزَّنة على مسار يُفتح دائماً
`GET /api/bot/identity` كان ينادي Discord مرتين (`/users/@me` و`/applications/@me`)
**بلا أي كاش** في كل طلب — ومسار Discord هذا محدود **لكل تطبيق لا لكل مسار**.
- `appearanceSnapshotCache` (TTL 60s، مفتاح = التوكن) + `invalidateAppearanceSnapshot()`
  بعد الحفظ. **قيس حياً: 2.400s بارد ← 0.415s مخزَّن (5.8×).**
- `GUILD_READ_CACHE_MS` 45s ← 60s لمطابقة المطلوب.
- **لا يوجد حلقة `useEffect`:** الاعتماديات `[guild.id]` ثابتة، والقياس أثبت أن كل
  مسار يُطلب بعدد ثابت (مرتان في وضع التطوير بسبب `StrictMode` فقط).
- قياس النداءات: 8 بارد، +2 للفتحة الثانية. الـ2 الباقيتان (`fetchBotMemberShared`)
  **مقصودتان** — بتة صلاحية قديمة تحوّل «البوت فقد الصلاحية» إلى «البوت يملكها».

### 3. التنظيف: ثوابت مكرّرة **تباعدت فعلاً**
`bot-preview.tsx` كان يُعيد تعريف `STATUS_LABELS`/`ACTIVITY_LABELS` بينما core يملك
`botStatusLabels`/`activityTypeLabels` (ويستعملها منتقي الحالة في الشاشة نفسها).
**النسختان تباعدتا:** المعاينة «لا تزعجني»/«غير مرئي» والقائمة المجاورة
«لا تُزعجني»/«غير ظاهر». حُذفت النسختان واستُوردتا من core.
`discord.js` مُتحقَّق: يُستورد في ملف واحد وبثوابته الرسمية.

### التحقق الحيّ
- الحزمة المخدومة `index-gDlxTapN.js` = `dist/assets/` بعد البناء، وتحمل العلامات
  الجديدة (`تعذّر عرض`, `إعادة المحاولة`) **وخالية** من النصوص المتباعدة.
- الحرّاس سليمة: بلا كوكي 401، وبجلسة وهمية `/api/bot/identity` 200 و
  `/api/guilds/{id}/customization` 401 (لا 500).
- **قيد صريح:** لا يمكن فحص مسار السيرفر حياً بجلسة المطوّر (توكن وهمي ⇒
  `loadUserGuilds` يفشل). الشاشة مُثبتة بـ`mount.test.tsx` بأشكال الحمولات الحقيقية
  المقروءة من مصدر المسارات.

## إغلاق المهمة — الحارس الإلزامي وبروتوكول التسليم (07:46)

- **`mount.test.tsx` ← 12/12 خضراء** بعد نقل إعداد jsdom إلى `test/dom-env.ts`
  واستيراده **أول سطر**. الاختبار 12 (قائمة الحالة + قائمة المدد الفرعية) كان يفشل
  بـ`the status menu opened 0 !== 1` رغم أن المُشغِّل يُعلن `data-state="open"` —
  السبب أن `react-dom` يلتقط الـhost globals **وقت الاستيراد**، فإعداد DOM داخل ملف
  الاختبار يجري **بعد** تقييم استيرادات الملف ⇒ React يبدأ بلا DOM والـportal لا
  يُثبَّت. نقل الإعداد إلى مديول يُستورد أولاً أصلحه فوراً.
- **`npm run verify` ⇒ exit 0، أربع مراحل كلها جرت** (lint → check:schema → test →
  build): **491 اختباراً، 0 fail، 0 skipped** — بوت 165 / لوحة 189 / core 137.
  (كان 473 قبل المهمة؛ +18: 12 في `mount.test.tsx` و6 في كاش الظهور.)
- **قاعدة `# skipped` احتُرمت:** تأكّد أن PostgreSQL يستمع على **55432** قبل التشغيل،
  فلا يتخطّى `storage.test.ts` بصمت.
- **إثبات أن الإصلاح وصل المستخدم فعلاً (لا المصدر فقط):**
  - البناء أنتج `dist/assets/index-DPJhAhGM.js` (542,841 بايت).
  - الخادم الحيّ على **3000** يخدم **نفس البصمتين** بالضبط.
  - جلبت الحزمة المخدومة بـHTTP وقارنتها: **`cmp` ⇒ IDENTICAL**، وفيها كل العلامات
    («تعذّر عرض»، «لا تُزعجني»، «غير ظاهر»، «معاينة حيّة»، «إعادة المحاولة») و**صفر**
    من المتغيّرتين «لا تزعجني»/«غير مرئي».
- **الدليل المرئي أُعيد توليده بعد آخر تعديل مصدري:** `identity-screen.html`
  (106,247 بايت) — القائمتان، المعاينة الحيّة، الصلاحيتان، **قائمتا الحالة والمدد
  (`role="menu"` ×2، `role="menuitem"` ×10 = 4 حالات [3 منها مُشغِّلات فرعية] + 6 مدد)**،
  و**0 أخطاء console**. المدد الست ظاهرة بنصوصها الحقيقية من core:
  «لمدة 15 دقيقة / لمدة ساعة / لمدة 8 ساعات / لمدة 24 ساعة / لمدة 3 أيام / دائم (Forever)».
- **تصحيح منهجي:** `grep -c` يعدّ **الأسطر** لا الوقوعات، والـHTML سطر واحد ⇒ كل
  العدّ كان يرجع 1 ظاهرياً. الصحيح `grep -o … | wc -l`.
- **قيد صريح (يتكرّر):** شاشة التخصيص **لا** يمكن فحص مسار سيرفرها حياً بجلسة المطوّر
  (توكن وهمي ⇒ `loadUserGuilds` يفشل في الحارس). الشاشة مُثبتة بـ`mount.test.tsx`
  بأشكال الحمولات الحقيقية المقروءة من مصدر المسارات، وبدليل الرسم المستقل.
- `MEMORY.md` ضُغط إلى **21,812 حرفاً / 237 سطراً** مع إضافة دروس jsdom الثلاثة
  (ترتيب الاستيراد، مُنشِئات jsdom، مسار النقر لفتح قائمة Radix)، وكل العلامات
  القديمة تأكّد بقاؤها.

## الإيداع والدفع (07:53)

- فحصت أولاً أن `apps/dashboard/` **خالٍ من أي ملف مؤقت** من بناء المعاينة (إعداد
  vite والمُدخل المؤقتان كانا في `.workbuddy-ai/preview/` المُتجاهَل)، و`git status`
  عرض بالضبط 11 معدّلاً + 3 جديداً بلا شوارد.
- **`git add -A` ثم فحص أسرار على الـstaged diff** (`discord_token|client_secret|
  password|BEGIN PRIVATE KEY|<bot-token-pattern>`) ⇒ **نظيف**.
- أُودع **`94cd2ec`**: «fix(dashboard): stop the black screen and the 429 storm at
  their roots» — 14 ملفاً، +1616/−352.
- **الدفع:** `gh` **غير مسجَّل** فـ`gh auth setup-git` لا يفيد، و`credential.helper`
  هو `wincred` (يحتاج GUI لا يراه shell غير تفاعلي) — وهذا أصل التعليق المسجَّل سابقاً.
  **الحلّ الذي نجح فوراً:** `GIT_TERMINAL_PROMPT=0 GIT_ASKPASS=/bin/true timeout 150
  git push origin main` ⇒ يفشل **فوراً** بدل أن يتعلّق أبداً، وwincred كان يحمل
  اعتماداً مخزَّناً فنجح الدفع في **4 ثوان** (`ed4fcfe..94cd2ec`).
- **التحقق بـ`git ls-remote origin refs/heads/main` ⇒ `94cd2ec`** (مطابق للـHEAD).
  **مرجع التتبّع كان مفقوداً** (`fatal: ambiguous argument refs/remotes/origin/main`)
  ⇒ أعدت كتابته يدوياً **آخر شيء** مع `branch.main.remote/merge`، والنتيجة
  `## main...origin/main` بلا «ahead»، والمرجع صمد بعد أوامر git قراءة-فقط.
- **مهارة جديدة `react-jsdom-mount-test`** (بلا أداة SkillManage في هذه الجلسة ⇒
  كُتبت مباشرة بالاتفاق نفسه: `agent_created: true`). تُوثّق: ترتيب الاستيراد الحامل
  للحمل، منشئات jsdom لا Node، الحمولات الحقيقية، القراءة قبل الـunmount، ترشيح ضجيج
  `act`، فتح قائمة Radix بالنقر، وإثبات أن الاختبار يكشف الانحدار برقمه.

## المواءمة المعمارية مع معايير ديسكورد (08:0x)

**مهمة مراجعة عليا:** مواءمة كاملة مع `discordjs/discord.js` و`discord/discord-api-docs`،
تطهير الكود الميت، وميثاق حوكمة. أُنجزت على أربع مراحل مع دليل قياس في كل خطوة.

### 1. المواءمة مع التعدادات الرسمية
- **المخالفة الحقيقية الوحيدة:** اللوحة كانت تكتب بتات صلاحيات Discord يدوياً
  (`USER_PERMISSIONS = { ADMINISTRATOR: 0x8n, ... }`) و`BOT_PERMISSIONS = "8"` —
  نسخة ثانية من أرقام يملكها discord.js، **وأحدها أخطأ فعلاً مرة** (التعليق نفسه
  يذكر MODERATE_MEMBERS بدل ADMINISTRATOR). البوت كان سليماً
  (`PermissionsBitField.Flags`) و`ChannelType`/`ActivityType` كذلك.
- **الإصلاح:** `packages/core/src/discord-permissions.ts` = **الاستثناء الوحيد المسموح**،
  مُصدَّر من `index.ts` **لا `browser.ts`** (قيم `bigint` = شأن خادمي). حُذف
  `USER_PERMISSIONS`/`BOT_PERMISSIONS` ونُقلت 13 موضع نداء إلى `DISCORD_PERMISSION_BITS`
  (4 ملفات + اختبار)، و`BOT_INVITE_PERMISSIONS` مُشتق من نفس البت.
- **`apps/bot/test/discord-standards.test.ts` (5 اختبارات):** يمسك قيم core مقابل
  `PermissionFlagsBits` و`ActivityType` الرسميين ⇒ **discord.js يبقى المصدر**.
  **أُثبت بإفساد `MANAGE_GUILD` عمداً: `not ok 1` مع «MANAGE_GUILD must equal
  PermissionFlagsBits.MANAGE_GUILD»**، ثم أُعيدت القيمة.

### 2. الكود الميت والتبعيات
- **حُذف 7 عناصر مؤكّدة الموت** (تحقّق مباشر بـgrep على المستودع كله، لا بثقة في السكربت):
  `invalidateUserGuildCache` (وتعليقها يدّعي موضعَي نداء غير موجودين — والحاجة مُغطّاة
  فعلاً بمفتاح الكاش على التوكن + `invalidateBotGuildCache` في callback الدعوة)،
  `PreviewGuildFooter` (مكوّن لا يُرسم، ومعه استيراد `cn` الذي لم يبقَ له مستعمل)،
  `commandsInCategory`، `BOT_IDENTITY_IMAGE_FIELDS`، `IMAGE_ASPECT_RATIOS`،
  `DISCORD_AUDIT_LOG_RETENTION_DAYS`.
- **⚠️ درس مهم:** `IMAGE_TARGET_SIZES` بدت ميتة، لكن `image-cropper.tsx` كان يعرّف
  **نسخة ثانية حيّة** منها (`CROP_TARGETS`) وهي التي ترسل البايتات فعلاً ⇒ **الحذف كان
  سيكون خطأ**. الصحيح: ربط المُقتطِع بـcore (`{ ...IMAGE_TARGET_SIZES.avatar, label }`).
  **لا تحذف ثابتاً «ميتاً» قبل أن تتحقق أنه ليس النسخة الصحيحة.**
- **3 تبعيات مُعلنة بلا استيراد** أُزيلت من اللوحة: `tailwindcss-animate` (القديمة،
  و`tw-animate-css` هي المستعملة في `index.css`)، `@radix-ui/react-visually-hidden`
  (تبعية وسيطة لـdialog، لا استيراد مباشر)، `dotenv`. **`dotenv` باقية في البوت** فعلاً.
- **⛔ تحقّق مهم:** كلا ملفَي Docker يشغّلان `npx tsx` في `CMD` ⇒ **`tsx` تبعية تشغيل
  حقيقية**، فنقلها إلى devDependencies يكسر النشر. لم أنقلها.
- **حلقة إعادة المحاولة في الواجهة سليمة:** `api/client.ts` تعيد **مرة واحدة فقط**
  (`mayRetry` تنقلب false) بسقف انتظار 5s. والمواقع الأربعة للمؤقتات كلها تُنظّف،
  و**لا وجود إطلاقاً** لـ`requestIdleCallback` أو أي Observer.

### 3. الفصل المعماري + ميثاق الحوكمة
- **core بلا أي تبعية npm**، واستيراده الخارجي الوحيد `node:crypto` في `security.ts`
  المستثنى من `browser.ts`. والـSPA لا تصل البوت ولا خادم الـBFF، والبوت لا يصل اللوحة.
  **فُرض هذا بثلاثة اختبارات جديدة** في `governance.test.ts`.
- **`docs/GOVERNANCE.md`: 23 → 27 قاعدة.** الجديدة: (24) المرجعية الرسمية الإلزامية،
  (25) ثوابت Discord من تعداداتها والاستثناء الواحد المُتحقَّق، (26) الـSnowflake نصّ
  دائماً، (27) لا تستهلك طلب Discord على بيانات يملكها البوت الحيّ + كاش TTL في الـBFF.
- **توسيع الفحص:** الإحالات كانت تُفحص في `apps/bot/src` فقط ⇒ إحالة فاسدة في core تمرّ.
  الآن تمسح **ثلاثة جذور** (`apps/bot/src` + `apps/dashboard/server` + `packages/core/src`).
  **أُثبت بإضافة `GOVERNANCE rule 99` في `core/security.ts` ⇒ `not ok 11` بالمسار الصحيح.**
- **انزلاق توثيقي أُصلح:** `cache.ts` قال «45 seconds» و`GUILD_READ_CACHE_MS` صار 60_000.
  و**README كان يعرض 158/166/128 = 452** مقابل الواقع. قِست المُتخطّى فعلاً بلا قاعدة
  (`DATABASE_URL` إلى منفذ ميت): **189 اختباراً، 165 نجحت، 24 تُخطّيت، exit 0** — فالعدد
  لا «ينخفض»، وهذا كان مضلّلاً في README.

### 4. التحقق النهائي
- **`npm run verify` ⇒ exit 0:** بوت **173** / لوحة **189** / core **137** = **499 اختباراً،
  0 fail، 0 skipped** (كان 473 في بداية اليوم). PostgreSQL على 55432 كان حياً قبل التشغيل.
- **إثبات وصول التغيير للمستخدم:** البناء أنتج `index-DFfM9Qx5.js`، والخادم الحيّ على 3000
  يخدم **نفس البصمة**، و`cmp` ⇒ IDENTICAL. وفي الحزمة المخدومة **صفر** من نص المكوّن
  المحذوف، وباقية عناصر المُقتطِع الحيّة وحدّ الخطأ.

## تصحيح المهارات — دليل الدفع كان خاطئاً (08:2x)

بعد الدفع راجعت المهارات القائمة فوجدت **تعليمات خاطئة تُعلَّم للمستقبل**، وهي أخطر
من نقص معلومة لأنها توكّد خطأً:

- **`gh auth setup-git` لا يعمل على هذا الجهاز.** المهارات وذاكرتا المشروع والمستخدم
  كانت تقول «شغّله مرة ثم ادفع». الحقيقة: `gh` **غير مسجَّل**، فيرد
  «You are not logged into any GitHub hosts» **ويخرج بـ0** — أي **فشل يبدو نجاحاً**.
  صُحّح في **أربعة مواضع**: `al-ai-git-recovery`، ذاكرة المشروع، ذاكرة المستخدم،
  و(سابقاً في هذه الجلسة) سلوك الدفع نفسه. الذي ينجح فعلاً ومُجرَّب مرتين اليوم:
  `GIT_TERMINAL_PROMPT=0 GIT_ASKPASS=/bin/true timeout 150 git push origin main`.
  وأُضيف تحذير: **لا تحلّ التعليق بـ`run_in_background`** — التعليق يبقى تعليقاً،
  والمهلة هي التي تحوّله لفشل مقروء.
- **`al-ai-stack-verify` كان يعلّم تشخيصاً خاطئاً:** «عدد اختبارات اللوحة نفسه هو
  المؤشر؛ بلا قاعدة حيّة يعطي رقماً أصغر (158 بدل 166)». **قِستُه فأثبتُّ العكس:**
  العدد **يثبت 189** في الحالتين، والفرق في `# pass`/`# skipped` (165/24 مقابل 189/0).
  وأُضيفت الطريقة الصحيحة للقياس بلا لمس القاعدة الحيّة:
  `DATABASE_URL="postgresql://x:x@127.0.0.1:1/x" npm test --workspace=@al-ai/dashboard`.
  **لا تُوقف PostgreSQL الحيّ لتقيس مسار التخطّي.**
- أُصلحت أعداد الاختبارات القديمة في المهارتين (452 → 499) وفي `al-ai-git-recovery`
  (457 → 499)، وأُضيف تنبيه «تأكّد أن PostgreSQL على 55432 قبل التشغيل».
- **أعداد السجلات اليومية المؤرَّخة تُركت كما هي** — تصف لحظتها، وإعادة كتابتها
  تزوير لا تصحيح.
