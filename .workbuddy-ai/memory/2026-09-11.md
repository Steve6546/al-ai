# 2026-09-11 — AL AI: مراجعة المشروع وتشغيله

## ما تم

- مراجعة كامل مستودع AL AI (apps/dashboard، apps/bot، packages/core، infra، docs/GOVERNANCE.md).
- إصلاح `.env`: كان **صفر بايت**، والمفاتيح الحقيقية كانت مكتوبة داخل `.env.example` المتتبَّع في Git.
  - نُسخت القيم إلى `.env` (متجاهَل الآن عبر `.gitignore`).
  - `.env.example` صار قالباً بقيم فارغة فقط.
  - أُزيل `.env` من فهرس Git بـ `git rm --cached` (الملف باقٍ على القرص).
  - دُوّرت المفاتيح الضعيفة (`SESSION_SECRET` كان sha256("test")) عبر `scripts/rotate-secrets.mjs`.
- استُثنيت مخلفات Lovable/Supabase من الفهرس عبر `.gitignore` (بدون حذف الملفات من القرص):
  `.lovable/ .tanstack/ .wrangler/ supabase/ /src/ /public/` + ملفات جذر قديمة.
- ثُبّتت الاعتماديات: `npm install` (127 حزمة). أُضيفت `fastify` و`@fastify/static` إلى `apps/dashboard/package.json`.
- أُضيف `apps/dashboard/src/vite-env.d.ts` (كان فحص الأنواع يفشل بسبب استيراد `styles.css`).
- البوت: `ready` → `clientReady` في `apps/bot/src/index.ts` (تحذير إهمال في discord.js v14.21+).
- تشغيل ناجح: اللوحة على `http://localhost:3000` (Fastify)، والبوت متصل بـ Discord كـ `ddas#9766` (id 1544750141112582187).
- التوكن صالح وموثّق؛ البوت عضو في سيرفر حقيقي واحد: `1540515175826985080`.

## نتيجة المراجعة: ما هو ناقص (لم يُبنَ بعد)

طبقة العقود/المعمارية مكتملة، لكن «الإصدار الوظيفي الأول» غير منفّذ:

1. `/auth/discord/callback` **غير موجود** — يردّ بـ 200 وصفحة SPA. تدفق OAuth مكسور.
2. لا جلسات (httpOnly/sameSite=lax/24h) ولا جدول `oauth_sessions` في `schema.sql`.
3. لا طبقة قاعدة بيانات إطلاقاً: لا حزمة `pg`، و`DATABASE_URL` غير مستخدم في أي ملف.
4. لا نقاط BFF للحفظ (التخصيص/السجلات) ولا فحص رتبة على الخادم، ولا nonce/HMAC بين الطبقات.
5. لا كاتب لـ Audit Trail ولا تشفير لـ `payload_ciphertext`.
6. البوت لا يسجّل أي معالج أحداث (لا join/ban/voice/role/message/channel/invite) — الاتصال فقط.
7. `EventPipeline`: لا debounce لحركات الصوت (المطلوب ثانيتان)، وحد 120/60s يُرجع بدون إعادة جدولة (المهام تتوقف بصمت).
8. لا supervisor ولا قفل نسخة واحدة ولا health checks في البوت.
9. اللوحة تعرض 3 سيرفرات وهمية دائماً (بلا فرع إنتاج)، وصفحة التخصيص تُظهر «الصلاحية متاحة» ثابتة، وقنوات السجلات أسماء وهمية.
10. `docs/GOVERNANCE.md` لا يمنع صراحةً إعادة نشر Slash Commands وقت التشغيل.
11. الاختبارات: اثنان فقط (2 pass). لا اختبارات OAuth/جلسة/HMAC/nonce/تشفير/توجيه قناة واحدة، ولا اختبار Docker تكاملي.
12. المستودع البعيد ما زال القديم: `Steve6546/glyph-weaver-forge.git` — لم يُنشأ مستودع AL AI جديد.

## قرارات

- لم تُحذف أي ملفات (التزاماً بتفضيل المستخدم): الاستثناء عبر `.gitignore` فقط.
- لم تُنشأ مستودعات بعيدة ولا حذف `.git` بدون تأكيد صريح.
